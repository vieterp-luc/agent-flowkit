# Phase 05 — Write `skills/fk-video-hardscaper.md` + Registration

## Context Links

- Overview: [plan.md](plan.md)
- Depends on: Phases 01-04 (all content this file assembles)
- Templates: `skills/fk-video-khkd.md` (structure), `setup.py` (registration)

## Overview

- **Priority:** P1
- **Status:** pending
- **Description:** Author the actual skill file by assembling Phases 01-04 into
  one coherent markdown skill, then register it (setup.py stub + CLAUDE.md row).

## Key Insights

- setup.py `discover_skills()` reads `skills/fk-*.md` line 1 as the description —
  line 1 MUST be a clean one-line `# fk-video-hardscaper — ...` heading.
- The `.claude/commands/fk-video-hardscaper.md` stub is AUTO-generated by
  `python3 setup.py --tool claude` — do NOT hand-write it.
- CLAUDE.md has a Skills table; a new row must be added manually (setup.py does
  not touch CLAUDE.md).
- Keep the file < ~330 lines — same band as khkd. Use tables, not prose.

## Requirements

### Functional

- `skills/fk-video-hardscaper.md` exists, self-contained, runnable end-to-end.
- `.claude/commands/fk-video-hardscaper.md` generated via setup.py.
- CLAUDE.md Skills table has a `/fk-video-hardscaper` row.

### Non-functional

- Follows `fk-video-khkd` structure; English; KISS; no MODE A dashboard.

## Architecture

### Skill file outline (`skills/fk-video-hardscaper.md`)

```
# fk-video-hardscaper — American Hardscaper Shorts Producer   ← line 1 (desc)

<1-2 line intro: one command → one finished 9:16 hardscaping Short>

Usage block (command + flags)            ← from Phase 01
## Pre-flight (/health + /fk-doctor)
## Identity table                        ← from Phase 01
## Pipeline overview (the 0-9 flow)      ← from plan.md
## Step 0 — Topic intake + framework lock
## Step 1 — Scene breakdown              ← Phase 02 (framework table + KB table)
## Step 2 — Create project (/fk-create-project, VERTICAL realistic)
## Step 3 — Scene images (/fk-gen-images) ← Phase 03 Imagen recipe + quota
## Step 4 — Scene videos (/fk-gen-videos) ← Phase 03 Veo recipe + probe/halt
## Step 4.5 — Review (/fk-review-video, regen weak scenes)
## Step 5 — Instrumental music (/fk-gen-music) ← Phase 04 Step A
## Step 6 — SFX layer                    ← Phase 04 Step B
## Step 7 — Text overlays (/fk-gen-text-overlays) ← Phase 04 Step D
## Step 8 — Beat-synced concat + mix     ← Phase 04 Step C + E
## Step 9 — Branding (/fk-brand-logo)    ← optional, --no-brand
## Output folder structure               ← from Phase 01
## Per-video checklist                    ← from Phase 06
## Common errors table                    ← from Phase 06
## When to call /fk-doctor
```

### Registration steps

1. `cd /Users/vieterp/code/Research/agent-flowkit && python3 setup.py --tool claude`
   → regenerates `.claude/commands/fk-*.md`, creating `fk-video-hardscaper.md`.
2. Verify `.claude/commands/fk-video-hardscaper.md` exists.
3. Add a row to the CLAUDE.md Skills table:
   `| /fk-video-hardscaper | Produce one American Hardscaper 9:16 Short (AI footage) |`
   — place it near the other `/fk-video-*` producer rows.

## Related Code Files

- CREATE: `skills/fk-video-hardscaper.md`
- GENERATED (do not hand-edit): `.claude/commands/fk-video-hardscaper.md`
- MODIFY: `CLAUDE.md` (add one Skills-table row)
- READ: `skills/fk-video-khkd.md`, `setup.py`

## Implementation Steps

1. Draft `skills/fk-video-hardscaper.md` per the outline, pulling each section's
   content from Phases 01-04.
2. Keep line 1 = `# fk-video-hardscaper — American Hardscaper Shorts Producer`.
3. Verify file < ~330 lines; trim prose to tables where possible.
4. Run `python3 setup.py --tool claude`; confirm the command stub generated.
5. Add the CLAUDE.md Skills-table row.
6. Sanity-check: `/fk-video-hardscaper` resolves as a command.

## Todo List

- [ ] `skills/fk-video-hardscaper.md` drafted (all sections from Phases 01-04)
- [ ] Line 1 is a clean one-line description heading
- [ ] File under ~330 lines
- [ ] `python3 setup.py --tool claude` run, stub generated
- [ ] CLAUDE.md Skills table row added
- [ ] Command resolves

## Success Criteria

- `skills/fk-video-hardscaper.md` is self-contained — an agent can run the whole
  pipeline from it alone.
- `.claude/commands/fk-video-hardscaper.md` exists and matches line-1 description.
- CLAUDE.md lists the skill.

## Risk Assessment

- **Risk:** setup.py mis-parses line 1. **Mitigation:** keep line 1 a single
  clean `#` heading, verify the generated stub's text after running.
- **Risk:** file too long / hard to follow. **Mitigation:** table-first, < 330 lines.

## Security Considerations

- None — no secrets in the skill file.

## Next Steps

- Phase 06 adds the checklist + error table content and runs the smoke test.
